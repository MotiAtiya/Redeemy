# Firebase — Development (redeemy-dev project)
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# Google Sign-In
GOOGLE_WEB_CLIENT_ID=

# EAS
EAS_PROJECT_ID=

# Firebase native credential files (not env vars — see README)
# iOS:  GoogleService-Info.plist  → project root (gitignored)
# Android: google-services.json  → project root (gitignored)
